<UserControl x:Class="Minutes.AudioVisualizerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Minutes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Width="800">
    <ItemsControl x:Name="AudioLevels" HorizontalAlignment="Center">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Rectangle Height="{Binding}" Width="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=BarWidth}" 
                           Fill="DarkOrange" 
                           Margin="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=BarMargin}" />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>
</UserControl>